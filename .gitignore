node_modules/
dist/
*.log
*.tsbuildinfo
.DS_Store
nul
coverage/
.env
.env.local
.vscode/settings.json

# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.idea/
*~

# Local-only editor / tooling config and working notes (not part of the product)
.claude/
.codex/
.cursor/
.windsurf/
.aider*
AGENTS.md
README-CODEX.md

# MCP servers' scratch output (Playwright MCP drops screenshots + snapshots
# into the cwd). These are throwaway verification artifacts — delete them, but
# ignore them too so a stray one never lands in a commit.
.playwright-mcp/
.mcp-output/

# Security scan outputs
scan.json
report.sarif
report.md
gate.json
safe.json
vuln.json
diff.json
diff-report.md

# Release artifacts (built locally for manual publish)
vibeguard-chrome-*.zip
vibeguard-aicoding-*.vsix
vibeguard-cli-*.tgz

# Local-only working documents and workspaces.
#
# These entries keep drafts, planning notes, measurement dumps and scratch
# working directories on the machine they were written on. They are not part of
# the product and are not intended to be published.
#
# TWO RULES ABOUT THIS BLOCK.
#
# It does not describe what any of them contain. An ignore rule is a public
# file, so an annotation explaining why something is withheld tells a reader it
# is worth having — the opposite of the point.
#
# It names as little as possible. Everything loose under docs/ is matched by
# extension rather than by filename, which was already true before any of the
# filenames were listed: `docs/*.md` has been here since the drafts block, so
# every individual .md line under it was matched twice and disclosed a title
# once. Published documentation lives in a subdirectory (docs/runbooks/), which
# these patterns do not reach. Directories still have to be named — a directory
# is not an extension — so the remaining names are the ones the mechanism
# requires, not the ones convenience left behind.
docs/*.docx
docs/*.pdf
docs/*.md
docs/*.txt
docs/*.html
docs/*.mjs
docs/reports/*.md
docs/weekly/
docs/manuscript/
paper_data/
dock/
security-experiment/

# Compiler-toolchain experiment workspace (local-only; synthetic fixtures,
# per-machine toolchain digests and regenerable measurement dumps. The real
# working copy lives on the WSL ext4 side — this entry keeps an accidental
# Windows-side copy out of the public repo)
compiler-experiment/

# Remotion product-explainer video project (local-only; the rendered mp4 and its
# ~500MB of Remotion/Chrome dependencies do not belong in the service repo)
video/

# Native build outputs under compiler/. The sources there are tracked, the
# artefacts are not: they are per-machine (they embed the local toolchain's
# layout), they are large, and an .o or .so that reaches a public commit cannot
# be taken back out of the history. Listed BEFORE the directory exists on
# purpose — the ordering is the point, not the paths.
compiler/**/build/
compiler/**/out/
compiler/**/bin/
compiler/**/*.o
compiler/**/*.so
compiler/**/*.a
compiler/**/*.elf
# A linked executable has no extension, so the patterns above cannot name it.
# The three output directories are the convention that makes it nameable; the
# ELF-header check in scripts/check-packaging-invariants.mjs is what catches a
# binary written anywhere else, loudly, instead of letting it be committed.

# Measurement output under compiler/, listed for the same reason and in the same
# order — before anything can write one. The rule those sources are written
# against is that measurements live on the Linux filesystem and never here: a
# recorded run embeds the local toolchain's digests and the absolute path of the
# tree it read, so a committed one is machine-specific noise at best and the
# layout of somebody's disk at worst. Intermediate IR and linker maps are on the
# list because they are the bulky, regenerable half of the same measurement.
#
# Fixtures are deliberately NOT listed. A fixture that turns up here is a
# decision someone made against the rule, and it should be visible in a diff and
# argued about, not swallowed silently by this file.
compiler/**/_lab/
compiler/**/_results/
compiler/**/__pycache__/
compiler/**/*.log
compiler/**/*.map
compiler/**/*.ll
compiler/**/*.pyc
compiler/**/*.bc

# Local-only maintenance tooling. Scripts here encode naming rules that this
# repository is not the right place to publish — a checker that lists what it
# forbids hands the list to anyone who reads it. They run before a push, not in
# CI; what CI enforces is in scripts/ and phrased in product terms.
scripts-local/

# Local-only measurement tooling. Its inputs and outputs are all ignored paths,
# so it is not runnable from a clone. Same rule as the block above: this file
# does not describe what any of it contains.
scripts/viz-*.mjs

# The design notes under docs/ are local-only, and the rule above matches them
# by extension — which means renaming one puts it back in reach of `git add .`.
# That is not hypothetical: one was renamed to an extension `docs/*.md` does not
# match, and it sat untracked-but-committable until
# scripts/check-disclosure-shape.mjs went red over it.
#
# ★ This paragraph used to state that file's size and list the kinds of thing it
# contained. That broke the first of the TWO RULES at the top of this block — an
# ignore file is public, and an annotation explaining what is withheld tells a
# reader it is worth having. Describing the contents to justify the rule is the
# same mistake as naming the document, and a size plus a list of subject matter
# is a stronger advertisement than a filename. The lesson survives without it:
# what matters is that RENAMING defeated an extension rule, not what the renamed
# file held.
#
# So the directory is ignored by shape rather than by extension, and the files
# that ARE meant to be published are re-admitted by name below. Adding a
# document to the repository is now a deliberate line in this file, which is the
# right amount of friction for the decision "this becomes public".
# The allow-list is exactly the set that is tracked today — no more. An earlier
# draft re-admitted two documents that had never been published, which turned a
# tightening into a leak of a different shape.
docs/*
!docs/DESIGN.ja.md
!docs/runbooks/

# wrangler's local state for the website in site/: dev-server cache, local KV,
# build metadata. Generated and machine-specific. The pragma census in
# scripts/check-packaging-invariants.mjs already skips `.wrangler`; this is the
# other half, so the directory stops showing up as untracked and stops being a
# candidate for `git add .`.
.wrangler/

# Written by `vibeguard --fix --write`: the fixer's record of what it inserted,
# read back by a later `--after-build` to check the repair survived the build.
# Machine-written and tree-local, so it is not a thing to commit — and this
# repository is scanned by its own CLI, so one would otherwise appear here.
.vibeguard/

# Build scratch for the ai-generated eval lane. The lane recompiles 840 files at
# five optimisation levels twice over; the assembly is regenerable and large.
compiler/eval/ai-generated/_build/
