name: Security Scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  security-events: write
  pull-requests: write

jobs:
  # Self-scan: VibeGuard runs against its own repo to populate the Security tab
  # and PR comments. Rule sources and test fixtures contain literal example
  # payloads (eval(), dummy credentials, ...) — those are masked with
  # `vibeguard:disable-file` / `disable-line` pragmas in the source so the
  # severity gate below can stay strict.
  #
  # `scripts/fixtures/` is exempted wholesale instead. It holds negative-control
  # corpora — the seeded exfiltration samples the M7 egress scanner is measured
  # against — whose entire reason to exist is to be detected. Failing the
  # self-scan on them is a tautology: the gate would be reporting that our
  # planted vulnerabilities are vulnerable. Per-file pragmas (the treatment used
  # for scripts/sec-a1-shape-check.mjs) don't fit here, because a pragma has to
  # name the rules it silences and the rules that will match these fixtures are
  # mostly the network-egress ones we haven't written yet; every new rule would
  # silently break CI until someone amended every fixture header. Directory-level
  # exemption keeps the fixture corpus free to grow.
  #
  # Severity gate:
  #   pull_request → fail on `high` or above
  #   push to main → fail on `critical` only (don't break main if a high slips
  #                   in; PRs should catch it first)
  #
  # A note on `--ignore fixtures`: the flag matches a directory *name*, not a
  # path (file-scanner walks entries and skips `ignore.has(entry.name)`), so
  # `--ignore scripts/fixtures` would be a silent no-op. `fixtures` is the only
  # directory of that name in the repo outside `paper_data/`, which is already
  # ignored, so the basename match is precise enough here — but it is a basename
  # match, and a future `packages/*/fixtures/` would be swept up with it.
  #
  # A note on `--ignore generated-corpus`: compiler/eval/ai-generated/generated-corpus/
  # holds 840 C files a language model wrote when asked to handle a secret. They
  # mishandle it — that is the measurement. The lane's whole result is the rate at
  # which the wipes in there fail to reach the artifact, and one of the two rule
  # defects it found was found BY scanning them. Letting the gate read them is the
  # same tautology as scripts/fixtures/: the PR gate would fail on `high` findings
  # that exist because we measured them into existence. Same basename caveat as
  # `fixtures` and `site` — `generated-corpus` is the only directory of that name
  # in the repo, so the basename match is precise, but it is a basename match.
  #
  # A note on `--ignore site`: site/ is the public website, and it is a page that
  # shows vulnerable code on purpose — the top page puts a Before snippet next to
  # the finding VibeGuard raises for it and the fixed After. That is the demo, not
  # a defect. Without this flag the self-scan reads those snippets, the push gate
  # fails main on `critical` and the PR gate blocks on `high`, and the Security tab
  # fills with findings against a marketing page. The snippets themselves are
  # generated at build time from samples/ (already excluded above) rather than
  # hand-written, so the payload strings live in generated site/src/data/*.json —
  # which is exactly why the directory, and not just the sources, has to be
  # excluded. Same basename caveat as `fixtures`: `site` is the only directory of
  # that name in the repo, and it sits at the root.
  self-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      - name: Pick severity gate
        id: gate
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "fail_on=high" >> "$GITHUB_OUTPUT"
          else
            echo "fail_on=critical" >> "$GITHUB_OUTPUT"
          fi

      - name: Run scanner (SARIF, non-failing)
        run: |
          node apps/cli/dist/index.js . \
            --mode standard \
            --format sarif \
            --out report.sarif \
            --ignore samples \
            --ignore test_problem \
            --ignore paper_data \
            --ignore fixtures \
            --ignore generated-corpus \
            --ignore site \
            --ignore dist \
            --ignore node_modules \
            --ignore .git \
            --ignore .github \
            --fail-on never

      - name: Run scanner (Markdown, non-failing)
        run: |
          node apps/cli/dist/index.js . \
            --format markdown \
            --out report.md \
            --ignore samples \
            --ignore test_problem \
            --ignore paper_data \
            --ignore fixtures \
            --ignore generated-corpus \
            --ignore site \
            --ignore dist \
            --ignore node_modules \
            --ignore .git \
            --ignore .github \
            --fail-on never

      - name: Upload SARIF to Code Scanning
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: report.sarif
          category: vibeguard

      - name: Post PR comment
        if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
        uses: marocchino/sticky-pull-request-comment@v3
        with:
          header: vibeguard
          path: report.md

      - name: Enforce severity gate
        run: |
          node apps/cli/dist/index.js . \
            --mode standard \
            --format json \
            --out gate.json \
            --ignore samples \
            --ignore test_problem \
            --ignore paper_data \
            --ignore fixtures \
            --ignore generated-corpus \
            --ignore site \
            --ignore dist \
            --ignore node_modules \
            --ignore .git \
            --ignore .github \
            --fail-on ${{ steps.gate.outputs.fail_on }}

  # Quality gate: rule correctness against curated samples.
  # samples/safe MUST stay clean (no false positives).
  # samples/vulnerable MUST keep tripping the expected number of rules.
  samples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      - name: samples/safe must produce zero findings
        run: |
          node apps/cli/dist/index.js samples/safe \
            --mode standard --format json --out safe.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('safe.json','utf8')).findings.length)")
          echo "samples/safe findings: $COUNT"
          test "$COUNT" -eq 0

      - name: samples/vulnerable must trip the rules
        run: |
          node apps/cli/dist/index.js samples/vulnerable \
            --mode standard --format json --out vuln.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('vuln.json','utf8')).findings.length)")
          echo "samples/vulnerable findings: $COUNT"
          test "$COUNT" -ge 15

      # VG-EMB 17g — the embedded corpus is a SEPARATE count from the web
      # samples above. It sits under samples/, so the self-scan's `--ignore
      # samples` already excludes it (no pragma needed). The exact per-rule
      # coverage is pinned in a vitest (embedded-samples.test.ts); CI enforces
      # the same floor/zero discipline as the web gate.
      - name: samples/embedded/safe must produce zero findings
        run: |
          node apps/cli/dist/index.js samples/embedded/safe \
            --mode standard --format json --out emb-safe.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('emb-safe.json','utf8')).findings.length)")
          echo "samples/embedded/safe findings: $COUNT"
          test "$COUNT" -eq 0

      - name: samples/embedded/vulnerable must trip the embedded rules
        run: |
          node apps/cli/dist/index.js samples/embedded/vulnerable \
            --mode standard --format json --out emb-vuln.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('emb-vuln.json','utf8')).findings.length)")
          echo "samples/embedded/vulnerable findings: $COUNT"
          test "$COUNT" -ge 18

      # 0.2.x — design-smell / supply-chain (design-*) and prototype-pollution
      # (proto-*) corpora, each a SEPARATE count from the web/embedded gates. Per-
      # rule coverage is pinned in design-samples.test.ts (E8); CI enforces the
      # same floor/zero discipline.
      - name: samples/design-safe must produce zero findings
        run: |
          node apps/cli/dist/index.js samples/design-safe \
            --mode standard --format json --out design-safe.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('design-safe.json','utf8')).findings.length)")
          echo "samples/design-safe findings: $COUNT"
          test "$COUNT" -eq 0

      - name: samples/design-smells must trip the design-smell and supply-chain rules
        run: |
          node apps/cli/dist/index.js samples/design-smells \
            --mode standard --format json --out design-smells.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('design-smells.json','utf8')).findings.length)")
          echo "samples/design-smells findings: $COUNT"
          test "$COUNT" -ge 6

      - name: samples/proto-safe must produce zero findings
        run: |
          node apps/cli/dist/index.js samples/proto-safe \
            --mode standard --format json --out proto-safe.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('proto-safe.json','utf8')).findings.length)")
          echo "samples/proto-safe findings: $COUNT"
          test "$COUNT" -eq 0

      - name: samples/proto-pollution must trip VG-INJ-020
        run: |
          node apps/cli/dist/index.js samples/proto-pollution \
            --mode standard --format json --out proto-pollution.json --fail-on never
          COUNT=$(node -e "console.log(JSON.parse(require('fs').readFileSync('proto-pollution.json','utf8')).findings.length)")
          echo "samples/proto-pollution findings: $COUNT"
          test "$COUNT" -ge 1

  # PR diff scan: scan only the lines this PR touches. Complements the
  # whole-repo self-scan above by giving reviewers a tight, low-noise list
  # of "what's new in this PR". Runs on PRs only.
  pr-diff-scan:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          # Need history on both sides of the diff.
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      - name: Run diff scan (Markdown for PR comment)
        run: |
          node apps/cli/dist/index.js \
            --diff "origin/${{ github.base_ref }}...HEAD" \
            --mode standard \
            --format markdown \
            --out diff-report.md \
            --ignore samples \
            --ignore test_problem \
            --ignore fixtures \
            --ignore generated-corpus \
            --ignore site \
            --fail-on never

      - name: Post diff-scan PR comment
        if: github.event.pull_request.head.repo.full_name == github.repository
        uses: marocchino/sticky-pull-request-comment@v3
        with:
          header: vibeguard-diff
          path: diff-report.md

      # samples/ and test_problem/ both contain intentionally vulnerable
      # fixtures that demonstrate what each rule catches; the dedicated
      # `samples` job above already validates samples/. Excluding both here
      # keeps the PR gate focused on real source changes (mirrors --ignore
      # samples / test_problem in self-scan). `fixtures` is excluded for the
      # same reason as in self-scan — scripts/fixtures/ is a negative-control
      # corpus that exists to be detected — and it matters more here, since the
      # PR that first commits a batch of seeded samples is exactly the PR the
      # diff gate would otherwise block. `--ignore` is honoured in diff mode
      # too: diff.ts skips a file when any of its path segments matches the
      # ignore set, the same basename semantics as the directory walk.
      - name: Enforce diff-scan severity gate (high)
        run: |
          node apps/cli/dist/index.js \
            --diff "origin/${{ github.base_ref }}...HEAD" \
            --mode standard \
            --format json \
            --out diff.json \
            --ignore samples \
            --ignore test_problem \
            --ignore fixtures \
            --ignore generated-corpus \
            --ignore site \
            --fail-on high
