{
  "name": "vibeguard-chrome",
  "displayName": "VibeGuard",
  "description": "Security diagnostics for AI-generated code, in any browser tab.",
  "version": "0.3.6",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs && node copy-static.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@vibeguard/analyzer-core": "0.3.6",
    "@vibeguard/findings-schema": "0.3.6"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "@types/node": "^20.19.43",
    "esbuild": "^0.28.2",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
